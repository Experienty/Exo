/*using System;
using System.Linq;
using System.Collections.Generic;

namespace Salarios
{
    public class Salario 
    {         
        static void Main()
        {
        	Console.Write("Numero de Fucionarios: ");
        	int.TryParse(Console.ReadLine(), out int Id);
        	Console.Write("Horas: ");
        	int.TryParse(Console.ReadLine(), out int H);
        	Console.Write("Salario: ");
        	double.TryParse(Console.ReadLine(), out double S);
        	var SALA = (S*H);
        	Console.Clear();
        	Console.WriteLine($"Number = {Id}\n");
        	Console.WriteLine($"Salary = {SALA:c}");
        }
    }
}*/