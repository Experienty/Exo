/*using System;
using System.Linq;
using System.Collections.Generic;

namespace Left
{
    class Lateral 
    { 
        static void Main()
        {
           const string frase = "digite o numero: ";
           Console.Write(frase.ToUpper());
       	   //Console.Write("Digite o numero: ");
       	   short.TryParse(Console.ReadLine(), out short x);Console.Clear();
       	   int fat = 1,val = x;
       	   while(x > 1)
       	   { 
       	   	   fat *= x;
       	   	   x--;
       	   }
       	   Console.ForegroundColor = ConsoleColor.Green;
       	   Console.WriteLine($"{val} = {fat}");
        }
    }
}*/