/*using System;
using System.Linq;
using System.Collections.Generic;

namespace By
{
    class Bynary 
    { 
    	static void Binary(int a)
    	{
    		if(a == 0){
    			Console.Write(a);
    		}else{
    			Binary(a/2);
    			Console.Write(a%2);
    		}
    	}
        static void Main()
        {
        	Console.Write("Enter The N°: ");
        	short.TryParse(Console.ReadLine(), out short x);Console.Clear();
        	Binary(x);
        }
    }
}*/