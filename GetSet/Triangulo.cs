/*using System;
using System.Linq;
using System.Collections.Generic;

namespace Trix
{
    public class Triangulo 
    {         
        static void Main()
        {
        	double A,B,C;
            double tempo;
        	
        	Console.WriteLine("digite o valor do A: ");
        	A = Convert.ToDouble(Console.ReadLine());
        	Console.WriteLine("digite o valor do B: ");
        	B = Convert.ToDouble(Console.ReadLine());
        	Console.WriteLine("digite o valor do C: ");
        	C = Convert.ToDouble(Console.ReadLine());
        	Console.WriteLine("---------------------");
        	/*Console.Write(A.ToString("F1"));
        	Console.Write(B.ToString("F1"));
        	Console.Write(C.ToString("F1"));/
        	
        	Console.WriteLine("O resultado do A: {0} e do B: {1} e do C: {2}\n",A,B,C);
        	
        	if(A < B){
        		tempo = A;
        		A = B;
        		B = tempo;
        	}else if(A < C){
        		tempo = A;
        		A = C;
        		C = A;
        	}else if(B < C){
        		tempo = B;
        		B = C;
        		C = tempo;
        	}
        	if(A >= B+C){
        		Console.WriteLine("Não Forma Triangulo\n");
        	}else{
        	   if(A*A == B*B + C*C)
        		Console.WriteLine("Triangulo Retangulo\n");
        	}
        	   if(A*A > B*B + C*C){
        		Console.WriteLine("Triangulo Obtusangulo\n");
        	}
        	   if(A*A < B*B + C*C){
        		Console.WriteLine("Triangulo Acutangulo\n");
        	}
        	
        	   if(A == B && A == C && B == C){
        		Console.WriteLine("Triangulo Equilatero\n");
        	}else{
        		if(A == B || A == C || B == C)
        		Console.WriteLine("Triangulo Isosceles\n");
        	}
        }
    }
}*/